apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    isDefault: true
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: scalers
      defaultBucket: telegraf
      tlsSkipVerify: true
    secureJsonData:
      token: your-token 


#GPT2 medium default

#Torch run 2 times

#keep on pulling nano gpt
